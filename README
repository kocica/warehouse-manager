# Requirements

 - g++8 (c++17, structured bindings, filesystem, ...)
 - Qt5
 - tinyxml2 (included in the sources)
 - matplotlibcpp (included in the sources)
 - SIMLIB/C++ (http://www.fit.vutbr.cz/~peringer/SIMLIB/)
 - C++SIM (https://github.com/nmcl/C--SIM)


# Usage

Configurator:
  ./whm_gui

Generator:
  ./whm_gen -o data/orders.xml -a data/articles.csv

Simulator (evaluate on test set of orders):
  ./whm_sim -o data/orders_test.xml -i data/locations.csv -l data/layout.xml

Optimizer (train on train set of orders):
  ./whm_opt -o data/orders_train.xml -a data/articles.csv -l data/layout.xml -i data/locations.csv -O 1-6      (see help)



# TODO

 - Dukaz TSP <-> SLAP
 - NN
 - CGP - Optimalizace layoutu
 - Pridat kazdemu conv rychlost / kapacitu pri vytvareni
 - Pridat dalsi optimazator - pruchod vsech moznosti? Pro porovnani pro male sklady
 - Item already added to the scene [UI]
 - Dalsi graf uz jen GAcko, protoze se ukazalo jako nejlepsi, a potom porovnani ruznych experimentu s GAckem
    [x] Classic
    [x] Params (+ prob mutation)
    [x] Params 2 (++ prob mutation crossover)
    [x] Reorder
    [ ] Picker vyssi vaha?
    [x] No trial, trial = 10, trial = 30 ?
    [x] mutationInverse/mutationOrdered
    [x] crossoverOrdered/crossoverHeuristic/crossoverBinomical
    [x] selectTrunc/selectTournam/selectRoulette/selectRank
 - Potom muzu ukazat nejlepsi kombinaci trial/mutace/crossover/select/reorder/parametry/...