# Requirements

 - g++8 (c++17, structured bindings, filesystem, ...)
 - qt5
 - tinyxml2 (included in the sources)
 - matplotlibcpp (included in the sources)
 - simlib


# Usage

Configurator:
  ./warehouse_manager_gui

Generator:
  ./warehouse_manager_gen -o data/orders.xml -a data/articles.csv

Simulator (evaluate on test set of orders):
  ./warehouse_manager_sim -o data/orders_test.xml -i data/locations.csv -l data/layout.xml

Optimizer (train on train set of orders):
  ./warehouse_manager_opt -o data/orders_train.xml -a data/articles.csv -l data/layout.xml -i data/locations.csv -O 1-6 (see help)



# TODO

 - Doplnit plot (nahodne razeni, slap)
 - Paralelizace smycky v optimalizatoru (simlib nejde paralelizovat)
 - Limity - cas, velikost
 - Dukaz TSP <-> SLAP
 - NN (pokud nebude fungovat paralelizace)
 - Doxygen

 + Funkce co vrati serazene produkty podle fast/slow moveru (heat)
 + Funkce co vrati serazene sloty podle hot/cold slotu (pomoci path finderu: gate start + gate shipping; sort)
 + Funkce ktera to rozradi podle SLAPu a vyblije nejlepsi slot/article alokaci

 - Disable buttons/etc. (whole control panel ???) until optimization / generating is over
 - Order count in simulator UI
 - ConfigParser "Field <%s> not found!" - exception - exception handling


 - When opt/gen/sim running, disable buttons
 - When going into thread "wait", show some waiting dialog
 - Resize of object - Cannot select due to incorrect mRect (has to move the item to recalculate)
 - Segfault when resizing objects and using ports/deleting
 - UI Optimizer selection: Random, SLAP (+ tabs for these)
 - Secure there are no "minus" generated ADUs